/** Borrador de `AddShoppingList` en sessionStorage: sobrevive a la navegación de ida y vuelta al
 * crear un producto genérico a medio formulario, y también se usa para prellenar la lista con los
 * productos que faltan al pulsar "A comprar" desde `Cook`. */
export const ADD_SHOPPING_LIST_DRAFT_KEY = 'kitchen:addShoppingListDraft'
